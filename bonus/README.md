# Movie Recommender (static)

This repo contains:
- `index.html` - static front-end (GitHub Pages friendly)
- `generate_recs.py` - Python script to create `recommendations.json` from `movies_metadata.csv`.
- `recommendations.json` - output (generate locally then add to repo).

How to generate:
1. Install deps:
